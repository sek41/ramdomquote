cconst button = document.getElementById('button'); // Corrected the method name from 'object' to 'getElementById'
button.addEventListener('click', function() {
    alert('Button clicked'); // Corrected the syntax for displaying an alert
});
